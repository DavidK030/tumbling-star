{
  "name": "tumbling-star",
  "version": "1.0.0",
  "private": true,
  "description": "Tumbling training for kids",
  "scripts": {
    "build": "./build.sh",
    "ios": "npm run build && npx cap sync ios && npx cap open ios",
    "server": "node server/server.js"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
