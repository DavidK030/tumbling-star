# Exercise clips stay out of the public repo by default: they show a child.
# Decide deliberately before publishing any of them (see videos/README note).
videos/*.mp4
videos/*.mov

# Children's progress lives here — never commit it.
server/data.json
server/data.json.tmp

# Build output and native project scaffolding
node_modules/
www/
ios/
!ios/App/App/Assets.xcassets
DerivedData/
server/videos/
